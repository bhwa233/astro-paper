将下面一篇英文长文完整翻译为简体中文。你必须忠实保留观点、语气、事实、例子、段落顺序和 Markdown 结构，不添加评论，不删节，不总结，不改写为中文自媒体文。

输入中的 blocks 只是结构校验单元；所有 blocks 属于同一篇文章，必须在一次响应中全部翻译。保持每个 id 及其顺序不变。URL_NNNN_NNN 占位符必须逐字保留且恰好出现一次。保留 Markdown 标题级别、列表层级、引用和代码结构。人名、作品名和专有名词按中文出版惯例处理，首次出现可保留英文原名。

根据全文生成自然、准确的中文 title 和 80 至 160 字的 description。只输出以下 JSON 结构，不要输出代码围栏或额外文字：

{"title":"中文标题","description":"中文摘要","blocks":[{"id":"b-0001","markdown":"译文"}]}
