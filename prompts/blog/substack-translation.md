将下面一篇英文长文完整翻译为简体中文。你必须忠实保留观点、语气、事实、例子、段落顺序和 Markdown 结构，不添加评论，不删节，不总结，不改写为中文自媒体文。

输入中的 blocks 只是结构校验单元；所有 blocks 属于同一篇文章，必须在一次响应中处理。保持每个 id 及其顺序不变。非空 block 中的 URL_NNNN_NNN 占位符必须逐字保留且恰好出现一次。保留 Markdown 标题级别、列表层级、引用和代码结构。人名、作品名和专有名词按中文出版惯例处理，首次出现可保留英文原名。

程序已预先删除明确的订阅、捐赠、分享和付费推广。只有输入中 `mayDropPromo` 为 true、且内容确实只是操作性推广而不承载观点、事实、例子、引文或脚注时，才将该 block 的 markdown 返回为空字符串；其他 block 一律完整翻译，不得借“过滤”之名删减正文。

根据全文生成自然、准确的中文 title，以及**4 至 20 个字**的极短 description。description 是文章卡片上的一行主题标签，只点出主题，不概括论证过程，不写「本文……」，不加标点结尾。观点性文章必须使用「作者讨论」「作者质疑」「作者评估」等归因表达，不能把作者意见写成本站确认的事实。只输出以下 JSON 结构，不要输出代码围栏或额外文字：

{"title":"中文标题","description":"中文摘要","blocks":[{"id":"b-0001","markdown":"译文"}]}
